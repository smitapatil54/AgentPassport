[project]
name = "agentpassport"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "smitapatil54", email = "smitapatil20454@gmail.com" }
]
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.52.4",
]

[project.scripts]
agentpassport = "agentpassport:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
